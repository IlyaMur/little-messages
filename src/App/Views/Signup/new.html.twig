{% extends "base.html.twig" %}

{% block title %}Sign up
{% endblock %}

{% block body %}

	<h1>Sign up</h1>

	{% if user.errors is not empty %}
		<p>Errors:</p>
		<ul>
			{% for error in user.errors %}
				<li>{{ error }}</li>
			{% endfor %}
		</ul>
	{% endif %}

	<form id="formSignup" action="/signup/create" method="post">
		<div class="form-group">
			<label for="inputName">Name</label>
			<input value="{{ user.name }}" id="inputName" name="name" placeholder="name" type="text" autofocus required class="form-control">

		</div>
		<div class="form-group">

			<label for="inputEmail">Email address
			</label>
			<input value="{{ user.email }}" id="inputEmail" name="email" placeholder="email address" type="email" required class="form-control">

		</div>
		<div class="form-group">

			<label for="inputPassword">Password
			</label>
			<input id="inputPassword" name="password" placeholder="password" type="password" required class="form-control">

		</div>

		<button class="btn btn-primary" type="submit">Sign up</button>


	</form>

{% endblock %}
